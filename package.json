{
  "name": "koco-validation",
  "version": "1.1.0",
  "authors": [
    "Frédérik Charest & Maxime Séguin"
  ],
  "description": "A set of utilities to help with Bootstrap & Knockout-Validation.",
  "main": "src/validation-utilities.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "jquery": "^2.1.3",
    "bootstrap": "^3.3.2",
    "knockout": "^3.3.0",
    "requirejs": "^2.1.15",
    "knockout-validation": "^2.0.1",
    "lodash": "^3.0.0"
  }
}
